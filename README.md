# Decentralized-Voting-System
A decentralized voting system for a fixed set of voters, implemented on a public blockchain. Each voter receives a unique cryptographic credential and can cast exactly one vote, enforced by a smart contract.
